# Cross-audit — 2026-06-01 (post-v0.66.0)

A0–A6 cross-audit per the TCE protocol, after the v0.66.0 release (instance-level traceability / OQ-129, closing P15). Scope: the state of `main` at the v0.66.0 bookkeeping commit. This is the **second** release on 2026-06-01 (after v0.65.0 / P2).

| Check | Finding |
|---|---|
| **A0 — Self-audit** | The spec describes what is built. OQ-129 is an Architecture-tier `:tested` entry backed by a real new engine module (`trace_instances.py`), a CLI subcommand, 21 tests, and a CI gate GitHub executes. The honesty bound (OQ-080) is restated in the OQ-129 notes (proves links resolve, not that a mitigation mitigates). ✓ |
| **A1 — Coverage** | 113 / 113 spec entries have registry rows. OQ-129 added to `ENGINE_SPEC.md` (entry + status summary + version history) and `artifact_registry.md` (row + coverage counts). ✓ |
| **A2 — Status & logic parity** | Registry status matches spec status for all 113 entries. OQ-129 is `:tested` in both. ✓ |
| **A3 — Evidence exists** | Every non-dash path in the OQ-129 registry row exists on disk: `engine/openqms/trace_instances.py`, the `_cmd_trace_instances` block in `cli.py`, `engine/tests/test_trace_instances.py`, `examples/trace-instances/` (8 Tier-1 + RMF-CARDIO-0100 Tier-2 + issues-export.example.json), `trace-policy.example.yaml`, the migrated RMF template, the 3 issue forms, `docs/guide/instance-traceability.md`. Verified by the green CI run. ✓ |
| **A4 — Status honesty** | OQ-129 is `:tested` (example-tested) — 21 hand-written pytest cases + a CI instance-trace gate, not property tests. Architecture-tier is correct (new engine surface: module + CLI subcommand), consistent with OQ-067/115/116/117/126. No overclaim. ✓ |
| **A5 — Stale counts** | Reconciled this release: spec total 112 → **113**; `:tested` 101 → **102**; dashboard status summary + masthead (v0.66.0) + `artifact_registry.md` coverage + changelog all updated. **Also reconciled a pre-existing drift:** `engine/uv.lock` recorded the openqms package version as **0.54.0** while `engine/pyproject.toml` said 0.64.0 — `uv sync --frozen` tolerates a stale editable-root version (which is why CI never flagged it), but it was wrong; bumped to **0.66.0** in lockstep with the pyproject + `__init__` engine-version bump (0.64.0 → 0.66.0, this release's engine-code addition). **Residual A5 items (carried, pre-existing, not introduced here):** (1) `dashboard.md` §Status summary still carries a stray duplicate `\`:verified\` \| 0` row — cosmetic, deferred to a hygiene pass. (2) `README.md` + `docs/modules-catalog.md` scope cells not re-counted — sweep when next touched. |
| **A6 — Test sync** | Green and genuinely running (post the v0.65.0 CI fix). The full `engine-tests` pipeline executes on every push/PR: lint (119 modules / 113 templates) · **286 pytest** · ~300 validate composites · coverage 100% · clause-level trace 0-orphans · **instance-level trace gate** (`trace-instances` on the worked example, 12 records / 16 edges / 0 errors). **Branch protection on `main` now requires the `engine-tests` check** (enabled 2026-06-01) — a dead pipeline can no longer go unnoticed. A6 holds, now with enforcement. |

## Forward-work status after v0.66.0

**All 15 compliance-architecture forward-work priorities are closed — the queue is empty.** P15 (instance-level trace network) was the last hard item; `openqms trace-instances` now spans Tier-1 frontmatter, Tier-2 in-body item tables, and the GitHub-issue substrate, gated in CI.

Next work is **net-new scope** (not forward-work closure): by maintainer intent, a **finance vertical** (8th vertical) — to be authored trace-ready against the now-complete instance-trace engine.
