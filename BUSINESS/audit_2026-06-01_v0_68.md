# Cross-audit — 2026-06-01 (post-v0.68.0)

A0–A6 cross-audit per the TCE protocol, after v0.68.0 (the **finance vertical** / OQ-130 — SOX / ICFR, the 8th vertical). This is the fourth release on 2026-06-01 and the first **net-new scope** beyond the now-closed 15/15 forward-work arc.

| Check | Finding |
|---|---|
| **A0 — Self-audit** | The spec describes what is built. OQ-130 is a Module-tier `:tested` entry backed by `modules/finance/module.yaml` (16 clauses), 6 templates, 4 tests, and CI validate steps GitHub executes. ICFR earns vertical status because it is a *build → bind → test → evaluate deficiencies* control framework — the same shape the engine already models. ✓ |
| **A1 — Coverage** | 114 / 114 spec entries have registry rows. OQ-130 added to `ENGINE_SPEC.md` (entry + status summary + version history) and `artifact_registry.md` (row + coverage counts). ✓ |
| **A2 — Status & logic parity** | Registry status matches spec status for all 114 entries. OQ-130 `:tested` in both. The public surface (README/index/catalog) was updated in lockstep this release — applying the v0.67.0 lesson that public docs must be swept every release, not just BUSINESS/. ✓ |
| **A3 — Evidence exists** | Every non-dash path in the OQ-130 row exists on disk: `modules/finance/{module.yaml,README.md}`, the 6 `templates/qms-finance/*.md`, the 4 new `registry/standards.yaml` entries, `engine/tests/test_finance.py`, `docs/guide/finance-sox-icfr.md`. Verified by the green CI run. ✓ |
| **A4 — Status honesty** | OQ-130 `:tested` (example-tested) — 4 hand-written tests + CI validate composites. Module-tier is correct (a regulatory module). OQ-080 firewall restated in the entry: ships the ICFR scaffold + testing/deficiency lifecycle; does not assert a control mitigates its risk nor substitute for the §404(b) auditor attestation. ✓ |
| **A5 — Stale counts** | Updated consistently across ENGINE_SPEC + dashboard + artifact_registry + **README + docs/index.md + docs/modules-catalog.md** (the public surface, synced same release): spec 113 → **114** · `:tested` 102 → **103** · modules 119 → **120** · templates 113 → **119** · clause→template bindings 1,012 → **1,031** · registry standards 140 → **144** · verticals 7 → **8** · and the index CLI-subcommand count (caught stale at 10 → **11**). **Residual (carried):** the `dashboard.md` duplicate `\`:verified\` \| 0` row (cosmetic); README directory-tree module enumeration (non-numeric, illustrative). |
| **A6 — Test sync** | Green + branch-protection-required. `engine-tests`: lint (120 modules / 119 templates) · **290 pytest** · validate composites incl. the 4 new finance ones · coverage 100% · clause-trace + instance-trace 0-orphan. Engine version unchanged at 0.66.0 (finance is content — no `engine/openqms/*.py` change), consistent with the v0.65.0 precedent for content releases. ✓ |

## Forward-work status after v0.68.0

The compliance-architecture forward-work arc remains **15/15 closed**. The finance vertical is the first deliberate **net-new scope** beyond it. Candidate follow-ons (maintainer's call, all forward): finance **class/market overlays** (EU CSRD/ESEF; broker-dealer SEC+FINRA; AML/BSA; banking prudential Basel III) — each authored trace-ready against the complete instance-trace engine, the same way the deficiency log already is.
