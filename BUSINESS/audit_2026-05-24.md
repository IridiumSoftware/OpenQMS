# Cross-audit findings — 2026-05-24

**Auditor:** Claude Opus 4.7 (Aaron + auto-driven)
**Scope:** Full A0-A6 cross-audit per CLAUDE.md cross-audit protocol. Previous audit dated 2026-05-22; since then 4 releases shipped (v0.35.0 + v0.36.0 + v0.37.0 + v0.38.0) adding 11 modules + 12 templates + 14 standards + 4 spec entries (OQ-105, OQ-106, OQ-107, plus the omnibus companion at v0.35.0).
**Engine version at audit time:** 0.38.0

## Summary

**Status:** Two critical findings, several minor drifts. None blocking; all remediable.

| Finding | Severity | Status |
|---|---|---|
| F1 — Registry missing 5 rows OQ-100..OQ-104 | Critical | Open |
| F2 — Example bundles count claimed 12; actual 8 | Critical | Open |
| F3 — Catalog section headings stale (class overlay count + cross-cutting count + example bundles count) | Minor | Open |
| F4 — OQ-068 stale notes ("13 of 15 subdirs populated") | Minor | Open |
| F5 — Templates count `81+` vs actual 87 | Trivial | Note |
| F6 — Medical-devices class overlay count ambiguity (9 vs 10 depending on `ivd` categorisation) | Trivial | Note |
| F7 — `companion_index.md` row for omnibus says "v0.24.0 through v0.35.0"; the file itself says "v0.24.0 through v0.34.0" | Trivial | Note |

## A0 — Self-audit (CLAUDE.md vs. observable practice)

**Status:** PASS. CLAUDE.md is in `/Users/aarongreen/Desktop/triadic-coordination-engine/CLAUDE.md` — not this project. The Open QMS project follows TCE evidence-discipline (S-IDs, tiers, statuses, evidence types, registry rows) verbatim. No drift in protocol-vs-practice.

## A1 — Coverage (every S-ID has a registry row)

**Status:** FAIL — 5 unintentional missing rows.

- Spec entries: 91
- Registry rows: 81
- Gap: 10 entries

Of those 10:
- **OQ-054..OQ-058 (5 entries)** — intentionally condensed per registry footnote: *"per-row table entries for OQ-054..OQ-058 condensed to keep the snapshot compact"*. NOT a finding.
- **OQ-100..OQ-104 (5 entries)** — **FINDING F1**. These were added to the spec at v0.26-v0.33 but full registry rows were never authored. The registry coverage-check footnote *mentions* their addition by-version but no individual rows exist.

**Remediation:** Add 5 rows to `artifact_registry.md` for OQ-100..OQ-104.

## A2 — Logic & Status parity (spec vs. registry)

**Status:** PASS for spot-check sample (OQ-001 / OQ-100 / OQ-105 / OQ-106 / OQ-107).
- OQ-001 spec `:verified`; registry... missing row (covered by F1 if expanded)
- OQ-100 spec `:tested`; missing row (F1)
- OQ-105 spec `:tested`; registry `:tested` ✓
- OQ-106 spec `:tested`; registry `:tested` ✓
- OQ-107 spec `:tested`; registry `:tested` ✓

**Caveat:** A2 cannot be exhaustively verified for the 5 missing rows (F1). After F1 remediation, re-run A2 spot-check.

## A3 — Evidence exists (every Test/Proof + Source file in registry exists on disk)

**Status:** PASS for OQ-105/106/107 spot-check (17 files verified — all present on disk).

Did NOT exhaustively verify all 81 existing registry rows. Targeted re-check on F1 remediation rows recommended.

## A4 — Status honesty (no `:proved`/`:verified` with weaker evidence type)

**Status:** PASS.

All 6 `:verified` entries have `evidence type: property-tested` (acceptable for `:verified` per CLAUDE.md table). Zero `:proved` entries exist. Zero `:verified` entries have `manual` / `none` / `example-tested` evidence type.

Entries: OQ-001 / OQ-002 / OQ-010 / OQ-011 / OQ-013 / OQ-015 — all property-tested, all `:verified` — clean.

## A5 — Stale counts

**Status:** FAIL — multiple drifts.

### F2 — Example bundles count

**Critical drift.** README + catalog both claim **12 example bundles**; actual count on disk = **8**.

```
bundles/example-aircraft.yaml
bundles/example-cart.yaml
bundles/example-drug-product.yaml
bundles/example-food-processor.yaml
bundles/example-machine-shop.yaml
bundles/example-samd.yaml
bundles/example-specialty-chemical.yaml
bundles/example-vehicle.yaml
```

Catalog inconsistency: scope table at top says **12**; `## Example bundles (N)` section heading at line 146 says **11**. Two different wrong numbers in the same file.

CI regenerates 8 bundles (matches reality). The claims-vs-reality gap is documentation-only — no functional regression.

### F3 — Catalog section headings stale

- `## Class overlays (38 across 5 verticals)` at line 50 — should be **44 across 6 verticals** (chemicals class overlays added at v0.38.0 — scope table top of file correctly updated; section heading missed)
- `## Cross-cutting overlays (19)` at line 118 — should be **23** (chemicals-adjacent standalones added at v0.37.0)
- `## Example bundles (11)` at line 146 — should be **8** per F2

### Other minor count items (trivial)

- F5: README/catalog "Document templates: 81+" vs actual `find templates -name "*.md" | wc -l` = 87. The `+` qualifier covers it but the gap has grown — could refresh to "87".
- F6: README breakdown "medical 9" class overlays vs disk-grep 10 (samd + implantable + ivd + ivdr-c + ivdr-d + mdr-class-iia/iib/iii + fda-class-ii/iii). Depends on whether `ivd` is categorised as "vertical-subdomain" (catalog narrative explicitly says so) or class overlay (disk position). Borderline; not material.
- F7: `companion_index.md` row 25 says omnibus covers "v0.24.0 through v0.35.0"; file itself says "v0.24.0 through v0.34.0" in `## §1 Computational basis`. Minor inconsistency — v0.35.0 was added as a coverage note inside the omnibus during the v0.35.0 commit but the file's primary heading + first-section span were not updated.
- F4: OQ-068 entry in spec — last note "8 of 15 → 11 of 15 → 13 of 15 populated" reflects state at v0.10.0. Actual current state: 75 subdirs populated (massive expansion since v0.10.0); zero empty subdirs. OQ-068 claim is now structurally obsolete — the "of 15" framing predates the current product-* + qms-* subdir tree.

## A6 — Test sync (every spec entry with a Test/Proof file is in CI)

**Status:** PASS for engine-test surface — `pytest engine/tests -q` collects + passes 108/108 tests. CI runs `pytest engine/tests -v` per `.github/workflows/engine-tests.yml`.

Module-tier entries are exercised via the CI `Validate shipped regulatory modules` step which validates every module (standalone + key composites) — 11 chemicals-domain modules + 4 adjacent + 6 class overlays all have validate-steps registered at v0.36-v0.38.

Bundle baselines exercised via CI `Regenerate example bundles` step — all 8 baselines clean per pre-commit check.

No CI gap.

## Remediation plan

| Finding | Action | Owner | Effort |
|---|---|---|---|
| F1 | Add 5 registry rows for OQ-100..OQ-104 + update coverage-check note + adjust "Total spec entries / Registry rows" arithmetic | This audit commit | ~15 min |
| F2 | Fix README + catalog `Example bundles` claim 12 → 8 (3 sites: README line 19, catalog line 16, catalog line 146 heading) | This audit commit | ~2 min |
| F3 | Fix catalog section headings (38→44, 5→6, 19→23, 11→8) | Same as F2 | included |
| F4 | Refresh OQ-068 spec entry to current reality OR explicitly retire the "of 15" framing | This audit commit | ~5 min |
| F5 | Optional: refresh template count "81+" → "87" | Skip — `+` qualifier holds; revisit at next major release | — |
| F6 | Optional: align "medical 9" vs disk-grep 10 — clarify `ivd` categorisation in README | Skip — borderline; not material | — |
| F7 | Fix omnibus heading + §1 span to "v0.24.0 through v0.35.0" | This audit commit | ~1 min |

## Re-audit cadence

Per CLAUDE.md: audits run "when integrating substantial new work, preparing a release, or after the spec is materially changed." Suggest next audit after the next 3 releases OR after the next vertical/major-architectural change, whichever comes first.

---

**Audit closure** — pending F1 + F2 + F3 + F4 + F7 fixes in the same commit as this findings doc. Post-fix re-run A1-A2 spot-check.
