# Cross-audit — 2026-06-01 (post-v0.67.0)

A0–A6 cross-audit per the TCE protocol. v0.67.0 is a **public-surface hygiene release** (docs-only; engine unchanged at 0.66.0), prompted by the maintainer observing the live GitHub README still showed v0.64.0. This is the **third** release on 2026-06-01 (after v0.65.0 / P2 and v0.66.0 / P15).

| Check | Finding |
|---|---|
| **A0 — Self-audit** | No spec changes; the spec already describes what is built (113 entries). This release only reconciles the *public-facing* surface (README + docs/) to that already-accurate spec. ✓ |
| **A1 — Coverage** | 113 / 113 spec entries have registry rows (unchanged from v0.66.0). ✓ |
| **A2 — Logic & status parity** | **Fixed a real parity break:** `docs/compliance-architecture.md` §Forward work still listed **P2 and P15 as open** ("Current effort distribution across remaining 2 open priorities … 2 hard (P2 + P15)"; P2 row "(next)"; P15 row "Future release") — while the spec/registry/dashboard recorded both as `:tested` and closed (OQ-128 v0.65.0, OQ-129 v0.66.0). Remediated: the effort line now reads "all 15 priorities closed; 0 open," and the P2/P15 rows are marked ✓ closed with their OQ + version. The dashboard derives its priority stack from this document, so the contradiction mattered. ✓ (now) |
| **A3 — Evidence exists** | Unchanged; all spec-cited paths exist on disk (verified at v0.66.0). ✓ |
| **A4 — Status honesty** | No status changes. ✓ |
| **A5 — Stale counts** | **The headline of this release.** The public surface had drifted from the BUSINESS/ tracking by ~6 releases — the A5 item flagged-but-deferred in both prior audits ("README + catalog scope cells not re-counted — sweep when next touched"). Corrected to current ground truth across `README.md` (v0.64.0 → v0.66.0; 111 → **113** spec, 105 → **113** templates, 116 → **119** modules, 379 → **1,012** bindings, ~135 → **140** standards, 10 → **11** CLI subcommands, + a Validation-family row), `docs/index.md` (v0.63.0 → v0.66.0; 110 → 113 spec, 105 → 113 templates, review baseline 0/116 → 0/119), and `docs/modules-catalog.md` (v0.64.0 → v0.66.0; same counts). Also fixed README architecture-tree comments frozen at "**80** spec entries / **57** templates / **108** tests / **63** standards" and the "Status (v0.22.0)" heading. Post-fix re-sweep: zero residual stale current-version/count assertions in the public tree. **Residual (carried, not introduced):** the `dashboard.md` duplicate `\`:verified\` \| 0` row remains (cosmetic); README's directory-tree module *enumeration* (vs counts) is illustrative and still omits later overlays — deferred as a non-numeric content refresh. |
| **A6 — Test sync** | Unchanged from v0.66.0 — engine untouched; `engine-tests` green + branch-protection-required. **Note:** this PR touches only `README.md` / `docs/` / `BUSINESS/`, none of which match the `engine-tests` path filter, so that required check does not run on it; it merges via admin override (the deliberate `enforce_admins=false` design for docs-only PRs). ✓ |

## Forward-work status after v0.67.0

Unchanged: **all 15 compliance-architecture forward-work priorities closed; queue empty.** Next is net-new scope — the finance vertical (8th vertical).
